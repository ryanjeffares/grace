func sqrt(n):
  if instanceof(n, float):
    return __NATIVE_SQRT_FLOAT(n);
  else if instanceof(n, int):
    return __NATIVE_SQRT_INT(n);
  end

  throw("Expected argument to `sqrt` to be an `Int` or `Float`");
end
