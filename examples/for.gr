func fib(n):
  if n < 2:
    return n;
  end
  return fib(n - 2) + fib(n - 1);
end 

func main():
  for i in 0..30:
    println(fib(i));
  end 

  var min = 0;
  var max = 100;
  for i in min..max:
    println(i);
    max = max - 1;
  end
end
