func fib(n):
  if n < 2:
    return n;
  end
  return (fib(n - 2) + fib(n - 1));
end 

func main():
  println(fib(5));
  println(fib(10));
  println(fib(20));
  println(fib(25));
  println(fib(30));
end

