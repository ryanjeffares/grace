import lib::lib;

class Person:
  var name: String;
  var age: Int;

  constructor(a, n):
    name = n;
    age = a;
  end
end

func main():
  final ryan = Person(23, "Ryan");
  final point = lib::lib::Point(1, 2);
  println(ryan);
  println(point);
end
