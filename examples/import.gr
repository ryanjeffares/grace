import std::math;
import std::time;

func main():
  lib::lib::say_hello();
  final start = std::time::time_us();
  for i in [0..100]:
    println(std::math::sqrt(i));
  end
  println("Found 100 square roots in " + (std::time::time_us() - start) + " us");
end
